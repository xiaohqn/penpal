# 心灵笔友（Mindful Copilot）工作汇报

日期：2026-05-06  
汇报范围：今日围绕产品化工作台的前后端联调、批量工作流、数据持久化、界面优化、模型接入与远程部署问题排查所完成的工作

## 一、工作目标

今日工作的主要目标包括以下几个方面：

1. 完善心理咨询 Copilot 工作台的批量处理流程，使其支持 Excel 导入后逐条处理，而不是一次性自动生成。
2. 增强数据沉淀能力，支持保存专家润色稿、专家批注，并支持后续导出。
3. 优化前端工作台的界面结构和视觉层级，使其更符合专家高频使用场景。
4. 排查并修复批量处理流程中的关键交互问题，例如风格选择无响应、完成当前条目无反应等。
5. 增强后端模型接入方式，支持在线 API 回复和本地模型路径回复两种模式。
6. 排查远程服务器部署后的前后端联通问题，补齐前端后端地址配置能力。

## 二、已完成的主要工作

### 1. 完成前后端基础产品化骨架

已基于原有 `planner_actor_system.py` / `plan.py` 思路，搭建出独立的产品化项目目录 `mindful-copilot/`，形成前后端分离架构：

- 后端：FastAPI
- 前端：React + Vite + Tailwind + TypeScript
- 数据库：SQLite

后端支持：

- 人格目录接口
- 流式生成接口
- 历史记录保存、列表、详情接口
- Excel 导入导出接口

前端支持：

- 来信输入
- 专家风格选择
- 多草稿对比
- 人工润色
- 专家批注
- 历史记录查看
- Excel 批量工作流

### 2. 批量 Excel 工作流改造完成

按照“专家逐条处理”的真实工作方式，已将批量功能调整为：

- Excel 仅导入问题列表，不再从 Excel 内预设风格
- 导入后每条来信进入工作台逐条展示
- 每条由专家手动选择风格
- 每条都可单独生成草稿、润色、添加批注
- 全部完成后导出“问题 + 最终回复 + 专家批注”

这一改造避免了“Excel 决定风格”的不合理流程，使系统更适合作为专家辅助工具使用。

### 3. 增加专家批注能力

后端已新增 `expert_annotation` 字段，并纳入：

- 保存记录
- 历史详情展示
- Excel 导出

前端工作台中已新增专家批注输入区，支持记录：

- 风格判断依据
- 风险判断
- 修改理由
- 对原草稿的结构化点评

### 4. 数据持久化能力确认并保留历史记录

已确认当前历史记录使用 SQLite 持久化存储，路径默认为：

- `mindful-copilot/data/app.db`

记录保存时通过 ORM 入库并 `commit`，因此：

- 重启前后历史记录不会自动清空
- 新记录会在原有基础上继续追加
- 只要不手动删除 `app.db`，历史数据会一直保留

同时明确了一个边界：

- 历史记录是持久化的
- 批量任务列表当前仍为前端页面内存态，不会跨网页关闭自动恢复

### 5. 修复批量处理关键交互问题

今日排查并修复了多个影响实际使用的问题：

#### （1）专家风格点击无反应

原因：

- 批量模式下默认选风格逻辑仍在运行，导致用户点击后立刻被状态重置

处理结果：

- 批量模式下不再自动选择默认人格
- 每条来信完全由专家手动选择风格

#### （2）点击“完成当前条目并进入下一条”无反应

原因：

- 前端保存逻辑中出现变量名错误：`user_input` 写成了不存在的变量，实际应为 `userInput`

处理结果：

- 已修复变量错误
- 前端构建验证通过
- 批量完成逻辑恢复正常

#### （3）完成当前条目后批量切换体验不顺畅

处理结果：

- 已支持上一条 / 下一条
- 已支持点击任意批量条目跳转
- 已支持已完成状态回显
- 已完成条目再次打开时可恢复最终回复和专家批注

### 6. 前端 UI/UX 重构完成一轮

针对“视觉层级平、布局效率低、色彩单调”等问题，已完成一轮更贴近专业工作台的界面改造。

主要改动包括：

- 将工作台改为更清晰的三栏工作流结构
  - 左侧：原始来信上下文
  - 中间：风格选择、草稿比较、润色与批注主流程
  - 右侧：批量任务侧栏
- 左侧原始来信区域改为粘性定位，便于全程对照原文
- 风格选择从普通标签提升为更强视觉识别度的卡片式选择
- 草稿区强化了当前草稿、生成状态、切换逻辑的可读性
- 底部操作条强化了 CTA 层级
- 整体视觉从偏米黄色改为更冷静、专业的低饱和灰蓝 + 暖白配色

### 7. 根据使用反馈继续精简界面

在完成第一轮视觉优化后，又根据实际体验做了进一步调整：

- 移除了“批量任务与处理节奏”那张说明卡，避免右侧信息冗余
- 修正左侧原始来信区域底部显示不全的问题
- 将“最后一步”操作条取消悬浮，改为普通模块，降低视觉压迫感

### 8. 增强后端模型接入方式

为支持更灵活的部署和测试，已将后端回复能力扩展为多模式可切换：

#### 已支持的回复模式

- `mock`：本地模板模拟，便于无 key 联调
- `api`：调用线上模型接口进行回复
- `local`：加载本地模型目录进行回复

#### 当前支持结构

- Planner：`mock` / `api`
- Generator：`mock` / `api` / `local`

#### 已新增配置项

- `PLANNER_MODE`
- `GENERATOR_MODE`
- `LOCAL_MODEL_PATH`
- `LOCAL_GENERATOR_MODEL_PATH`
- `LOCAL_DEVICE`
- `LOCAL_DTYPE`

并在健康检查接口中增加当前运行模式回显，便于确认实际走的是哪条模型链路。

### 9. 排查远程部署后前后端不联通的问题

问题现象：

- 项目搬到远程服务器后，前端页面无法显示人格列表和其他后端数据

根因分析：

- 前端原先依赖本地 Vite 代理，将 `/api` 转发到 `127.0.0.1:8000`
- 部署成远程静态页面后，这个代理链路通常失效
- 前端继续请求相对路径 `/api/...`，但请求未真正到达 FastAPI

处理结果：

- 前端新增 `VITE_API_BASE_URL` 支持
- 所有请求统一改为通过 `buildApiUrl()` 拼接目标后端
- 现在可在前端构建时显式指定远程后端地址

例如：

```bash
VITE_API_BASE_URL=http://your-server-ip:8000 npm run build
```

这一步显著提升了远程部署成功率，也为后续通过域名、反向代理等方式上线打下基础。

### 10. CORS 配置问题定位明确

在部署和联调过程中，进一步明确了跨域问题的根因：

- `main.py` 中的 `allow_origins=settings.cors_origins` 本身没问题
- 真正决定是否跨域允许的是 `.env` 或 `config.py` 中的 `cors_origins`

已明确推荐做法：

- 主要修改后端 `.env` 中的 `CORS_ORIGINS`
- 将常见前端地址（如 3000、5173、5174，及远程域名/IP）加入允许列表
- 修改后重启 FastAPI 服务

## 三、今日完成的验证工作

今日已完成的验证包括：

- 前端多轮 `npm run build` 构建验证，确保页面结构调整和请求地址调整后仍可正常打包
- 后端 `python3 -m compileall` 语法级检查，确认模型接入和配置改造后的 Python 代码无语法错误
- 本地开发流程下的人格列表、批量处理、记录保存、批注写入等链路的静态级与逻辑级验证

## 四、当前项目状态

截至今日，项目已具备以下阶段性成果：

- 具备可运行的心理咨询工作台前后端骨架
- 支持单条与批量两种处理模式
- 支持 AI 草稿生成、人工润色、专家批注、结构化存档
- 支持 Excel 导入逐条处理与 Excel 导出最终结果
- 支持历史记录持久化保存
- 支持线上 API 和本地模型两种回复接入思路
- 支持远程部署时显式指定后端地址

项目整体已从“研究脚本”迈入“可演示、可联调、可扩展的产品雏形”阶段。

## 五、尚未完成或建议后续继续推进的内容

为了提升系统稳定性与可用性，建议下一阶段继续推进以下工作：

1. 批量任务持久化
   当前批量任务列表仍是前端临时状态，建议后续把批次本身也落库，实现“关掉网页后继续处理”。

2. Planner 本地模型支持
   当前本地模型模式主要支持 Generator，后续可扩展 Planner 也支持本地模型。

3. 生产部署方案固化
   建议补一版标准部署文档，包含：
   - FastAPI 启动方式
   - 前端构建方式
   - Nginx 反向代理配置
   - CORS 与环境变量模板

4. 前端连接状态提示
   可在页面中增加“当前连接的后端地址”和“后端可用状态”提示，减少部署时排查成本。

5. 本地模型兼容范围扩展
   当前本地模式以 `transformers` 目录模型为主，后续可继续考虑兼容 `GGUF`、`Ollama` 或 `llama.cpp` 方式。

## 六、总结

今日工作的核心价值在于：不仅补全了工作台的关键使用链路，也针对真实使用中暴露的问题进行了多轮修复和体验优化。

从结果上看，系统已经具备了以下较完整的闭环能力：

- 导入问题
- 逐条选择风格
- 生成多草稿
- 专家润色
- 专家批注
- 保存高质量样本
- 批量导出结果

同时，项目在架构上也向更真实的部署环境迈进了一步，包括：

- 支持多种模型接入方式
- 支持远程部署显式配置后端地址
- 明确了 CORS 和前后端联调的关键控制点

整体来看，今日工作有效提升了该系统的可用性、可扩展性与产品化程度，为后续继续打磨成一个真正可供专家长期使用的 AI 协同工作台奠定了基础。
